name: "Build and Test Pull Request"

on:
    pull_request:
        branches: 
          - main
        paths:
          - '**.rs'

jobs:
    call-build-and-test:
        uses: ./.github/workflows/rust.yml
        
        